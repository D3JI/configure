<%= render :partial => 'top'%>
<div id="content">
  <div id="indexleft">
    <%= render :partial => 'shared/left'%>
  </div>
  <% if flash[:notice] %>
    <div class="alert"><%= flash[:notice] %></div>
  <% end %>
  <div id="con_ina">
    <div class="mod">
      <div class="modth">
        <div class="lc">
          <div class="rc">
            <div class="modtit">
              <h1>上传照片</h1>
            </div>
          </div>
        </div>
      </div>
      <div class="modbox" id="addblog">
        <div id="notice">
          <strong>请选择要上传的照片:</strong>
          一次最多可上传5张，每张大小在200k以内。您可以上传JPG、GIF和PNG格式的照片。
        </div>
        <% form_for :photo, :url => gallery_photos_path(:user_id => logged_in_user.id), :html => { :multipart => true } do |f|%>
          <table><tr>
              <strong>Upload a file:</strong>
              <%= f.file_field :uploaded_data %>
            </tr>
            <tr>
              <td>名称：</td>
              <td><label>
                  <%= f.text_field :title, :class => 'input' %>
              </label></td>
            </tr>
            <tr>
              <td>简介：</td>
              <td><label>
                  <%= f.text_area :description, :rows => 5, :cols => 45 %>
              </label></td>
            </tr>
          </table>
          <ul>
            <li>
            <strong>默认标签:</strong>
         <!--% tag_cloud @tags, %w(tag1 tag2 tag3 tag4) do |tag, css_class|%>
              < link_to tag.name, { :action => :tag, :id => tag.name } , :class => css_class %>
            < end %>-->
            </li>
            <li><strong>自己写标签: </strong>
            <input name="tags" id="tags" size="70" type="text">
            </li>
            <li><strong>已有自定义标签:</strong><cite> 提示：点击已有标签可以直接添加至标签栏。多个标签请用空格进行分割。若不填请留空。</cite></li>
            <li class="upload_notice">请勿上传涉及色情、政治敏感及其他非法内容的照片，否则会被删除。</li>
            <%= submit_tag 'Save', :class => 'button'%>
          </ul>
        <% end %>
      </div>
      <div class="clear"></div>
    </div>
  </div>
  <div class="clear"></div>
</div>
</div>

