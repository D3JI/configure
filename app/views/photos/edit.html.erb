<%= render :partial => 'top'%>
<div id="content">
  <div id="indexleft">
    <%= render :partial => 'shared/left'%>
  </div>
  <% if flash[:notice] %>
    <div class="alert"><%= flash[:notice] %></div>
  <% end %>
  <div id="con_ina">
    <div class="mod">
      <div class="modth">
        <div class="lc">
          <div class="rc">
            <div class="modtit">
              <h1>编辑照片</h1>
            </div>
          </div>
        </div>
      </div>
      <div class="modbox" id="addblog">
        <% form_for :photo, :url => gallery_photo_path, :html => { :multipart => true, :method => :put } do |f|%>
          <table>
            <tr>
              <td>图片:</td>
              <td><label>
                  <%= f.file_field :uploaded_data %>
              </label></td>
            </tr>
            <tr>
              <td>标题：</td>
              <td><label>
                  <%= f.text_field :title, :class => 'input' %>
              </label></td>
            </tr>
            <tr>
              <td>描述：</td>
              <td><label>
                  <%= f.text_area :description, :rows => 5, :cols => 45 %>
              </label></td>
            </tr>
          </table>
          <ul>
            <li><strong>默认标签:</strong>
            <a href="javascript:void(0)" onclick="att(this,'自然风光');return false">自然风光</a></li>
            <li><strong>自己写标签: </strong>
            <input name="tags" id="tags" value="" size="70" type="text">
            </li>
            <li><strong>已有自定义标签:</strong>
            <cite> 提示：点击已有标签可以直接添加至标签栏。多个标签请用空格进行分割。若不填请留空。</cite></li>
            <li class="upload_notice">请勿上传涉及色情、政治敏感及其他非法内容的照片，否则会被删除。</li>
            <li> <%= submit_tag 'Save', :class => 'button'%>
            <input name="aid" id="aid" value="0" type="hidden">
            <input name="action" id="action" value="save" type="hidden"></li>
          </ul>
        <% end %>
      </div>
      <div class="clear"></div>
    </div>
  </div>
  <div class="clear"></div>
</div>
</div>
