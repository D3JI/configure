<h1>Admin::Home#index</h1>
<h1>Listing users</h1>

<table>
  <tr>
    <th>用户名:</th>
    <th>注册时间:</th>

  </tr>
  <% for user in @users %>
    <tr>
      <td><%=h user.username %></td>
      <td><%=h user.created_at %> </td>
    </tr>
  <% end %>
</table>

<br />

